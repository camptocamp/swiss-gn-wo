# GN vanilla-wo
GeoNetwork vanilla - war overlay

# Starting with docker compose
As an example, we are providing [this docker compose file](docker-compose.yml) to start the 3 required containers: Postgres, Elasticsearch and GeoNetwork.

# Running the application
Please note that the chosen config-editor.xml implies that "Enable XLink resolution" must be checked.
